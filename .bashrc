[ "$PS1" ] && source ~/.bash_profile;

# pnpm is set in .path for m1 macs reinstall using pnINSTALL and leave config
# that will appear here but do not push to dotFiles repo
# only for non m1 arm (intel) macs (for example edgedb-cli would be the same)
